{
  "dependencies": {
    "axios": "^1.5.1",
    "install": "^0.13.0",
    "json-server": "^0.17.4",
    "npm": "^10.2.0"
  },
  "scripts": {
    "start:db": "npx json-server -w db.json",
    "start:parcel": "npx parcel index.html"
  },
  "devDependencies": {
    "buffer": "^6.0.3"
  }
}
